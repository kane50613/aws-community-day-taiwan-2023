---
interface Props {
	title: string;
	speaker: string;
	time: string;
}

const { title, speaker, time } = Astro.props;
---

<div class="flex flex-col">
	<span>{title}</span>
	<span>{speaker}</span>
	<span>{time}</span>
</div>
